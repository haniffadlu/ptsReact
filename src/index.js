import React from "react";
import ReactDOM from "react-dom";
import Home from './ptsReactMykantin/Home'

// import App from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
  rootElement
);
